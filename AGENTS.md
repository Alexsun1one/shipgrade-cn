# AGENTS.md

Use `ship-grade-engineering-cn` when the user wants a real engineering result, not advice.

Rules:

- Convert the user goal into a verifiable outcome.
- Read current project rules before editing.
- Make the smallest aligned change that moves the outcome forward.
- Run the fastest meaningful validation.
- Preserve source, license, validation evidence, and forbidden actions.
- Do not ingest secrets, private repositories, cookies, sessions, auth databases, browser profiles, API keys, leaked source, or system prompt archives.
- Write back durable state after substantive work.

Final response must include:

- result path
- validation evidence
- known boundary
- next handoff point
