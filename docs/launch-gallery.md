# Launch Gallery

These PNG assets are generated by `scripts/build_github_repo.py` and are safe to publish in the GitHub README.

| asset | purpose |
| --- | --- |
| `assets/shipgrade-loop.png` | First-screen workflow card: request -> engineering loop -> verifiable delivery. |
| `assets/shipgrade-terminal-demo.png` | Terminal proof card showing the commands a new user can run immediately. |

The images are generated from local release facts, not screenshots of private projects.
