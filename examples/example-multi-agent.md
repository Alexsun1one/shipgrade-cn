# Example: Multi-Agent

User asks: 让另一台机器也加入工作。

Ship-grade response:

1. 主控定义 shared goal 和 canonical paths。
2. worker 只做独立验证/训练/检索。
3. 禁止复制 auth.json、sessions、private keys、cookies。
4. 用 SSH alias、handoff、manifest 同步。
5. 主控合并并写回 Obsidian。
